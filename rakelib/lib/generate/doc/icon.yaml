---
:wxIcon.GetIcon:
  :detail:
    :pre:
      :programlisting:
        - :pattern: !ruby/regexp /.*/
          :replace: |

            ```ruby
              get_icon(Wx::Size.new(size,size))
            ```
